"use client";
import Link from "next/link"; import {Search, Bell, Menu} from "lucide-react"; import {usePathname} from "next/navigation"; import {Logo} from "@/components/logo";
export function Header(){const path=usePathname(); const active=(p:string)=>path===p||path.startsWith(p+"/"); return <header className="header"><Logo/><nav className="nav" aria-label="Primary navigation"><Link className={active("/")&&!active("/rankings")&&!active("/creators")&&!active("/submit")?"active":""} href="/">LIVE</Link><Link className={active("/rankings")?"active":""} href="/rankings">RANKINGS</Link><Link className={active("/creators")?"active":""} href="/creators">CREATORS</Link><Link className={active("/submit")?"active":""} href="/submit">SUBMIT</Link></nav><div className="header-spacer"/><Link href="/search" className="search-pill" aria-label="Open search"><Search size={16}/>Search by title or creator</Link><button className="icon-btn" aria-label="Notifications"><Bell size={18}/></button><Link href="/profile" aria-label="Open profile"><img className="avatar" src={"https://images.unsplash.com/photo-1494790108377-be9c29b29330?auto=format&fit=crop&w=120&q=80"} alt="Profile"/></Link><button className="icon-btn mobile-menu" aria-label="Open menu"><Menu size={19}/></button></header>}
